docker run -it \
  -e POSTGRES_USER="root" \
  -e POSTGRES_PASSWORD="root" \
  -e POSTGRES_DB="ny_taxi" \
  -v $(pwd)/ny_taxi_postgres_data:/var/lib/postgresql/data \
  -p 5431:5432 \
  --network=pg-network \
  --name pg-database \
  postgres:13

--------------------------------------- CREATE NEW TERMINAL FOR EACH CODE -------------------
  docker run -it \
  -e PGADMIN_DEFAULT_EMAIL="admin@admin.com" \
  -e PGADMIN_DEFAULT_PASSWORD="root" \
  -p 8081:80 \
  --network=pg-network \
  --name pgadmin \
  dpage/pgadmin4

--------------------------------------- DO this in Pgadmin  -------------------

Open : http://localhost:8081/browser/
Connect to the pg-database container:
Hostname: pg-database
Username: root
Password: root


--------------------------------------- CREATE NEW TERMINAL FOR EACH CODE -------------------
URL="https://github.com/DataTalksClub/nyc-tlc-data/releases/download/yellow/yellow_tripdata_2021-01.csv.gz"

python3 ingest_data.py \
  --user=root \
  --password=root \
  --host=localhost \
  --port=5431 \
  --db=ny_taxi \
  --table_name=yellow_taxi_trips \
  --url=${URL}


  --------------------------------------- DO this in Pgadmin  -------------------

SELECT table_name
FROM information_schema.tables
WHERE table_schema = 'public';

select count(*) from yellow_taxi_trips



-------------------- Incase you want to use new tool Dbeaver -----------------

localhost: localhost
database: ny_taxi
Port: 5431
Username: root
Password: root
